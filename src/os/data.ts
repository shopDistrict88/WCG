/**
 * Legacy data facade — use useData() from store/DataContext in components.
 * This file only re-exports types and the channels constant for compatibility.
 */
export const channels: string[] = []
